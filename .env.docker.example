# Opcional: solo para cambiar el puerto expuesto en el host (default 8080)
# APP_PORT=8080
#
# Las credenciales MySQL se leen de backend/.env (DB_HOST, DB_USER, DB_PASS, DB_NAME)
# No necesitas duplicar un .env en la raíz del proyecto.
